def reset_data(hard_reset: bool=False):
    """
    Reset the data for the project:
        - clear batch folders
        - recreate batches
    If hard_reset:
        - recreate production and new data
    """
    pass